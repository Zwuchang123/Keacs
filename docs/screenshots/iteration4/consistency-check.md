# 迭代4 UI 自检

- 文案：新增、编辑、转账、账单和统计页面未发现错别字、缺字或语义不清。
- 样式：继续使用浅色背景、蓝色主操作、白色卡片和圆形图标，和既有页面保持一致。
- 布局：360dp 级别宽度下未发现明显重叠、横向溢出或底部系统栏遮挡。
- 交互：新增保存、账单进入编辑、编辑保存、删除确认、返回导航和底部导航可用。
- 表单：金额为空或为 0 时保存不可用；转账账户相同时会提示；保存中会禁用重复点击。
- 状态：账单空态、账单有数据状态、统计同步状态、删除确认弹窗已覆盖。
- 离线：Manifest 未发现联网权限，核心流程使用本地数据库。
