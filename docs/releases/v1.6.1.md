# v1.6.1

- 修复助手频繁出现"模型服务暂时不稳定"的问题：移除对模型回复中"乱码""没能理解"等词的自动兜底触发条件，改为只有模型返回空内容或确认调用失败时才走兜底逻辑；同时将模型超时时间从 90 秒延长到 120 秒
- 移除模型输出长度限制：将 `model_max_completion_tokens` 从 700 提升到 4000，支持更长的回复输出
- 实现助手 Markdown 渲染：使用 commonmark 库解析并渲染助手回复中的 Markdown 格式（标题、加粗、斜体、链接、列表、代码块等），提升内容可读性