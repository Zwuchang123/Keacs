# v1.6.3

- 修复助手回复被截断的问题：提高服务端默认模型输出上限，并在模型返回半截 JSON 时改为可读兜底提示，避免 App 展示截断内容。
- 优化助手思考区：默认收起思考过程，收起状态展示本次思考耗时，流式处理中展示已用时间。
- 优化助手 Markdown 渲染：补充代码块处理和表格移动端分组展示，提升账本分析内容的可读性。
- 调整助手引导：空态默认展示引导内容且不可关闭；对话后的引导开关保持原交互。
- 修复用户消息长按菜单不可见的问题：菜单从用户气泡裁剪区域中移出，并补齐菜单背景和文字颜色。
- 提升官方助手服务稳定性：容器健康检查改为存活接口 `/health`，避免模型配置或上游波动导致服务被错误判定不可用；补充部署检查命令。
- 更新平台化文档框架：明确 “1 个 Agent + N 个功能服务 + 公共组件平台” 的长期方向，补充公共组件索引和模块文档框架。
