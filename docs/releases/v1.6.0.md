# v1.6.0

## 主要变化

- **Agent 对话执行 E2E 测试**：添加完整的 Agent 对话流程端到端测试用例和测试报告。
- **Agent 助手架构优化**：重构 Agent 助手架构，改进对话执行逻辑。
- **新功能模块**：新增备份服务、Excel 导入服务、定时记账等功能模块。
- **数据层重构**：全面重构数据层，包括数据库架构、DAO 层、Repository 层。
- **UI 组件库**：构建统一的 UI 组件库，包括 KeacsCard、KeacsBottomBar、KeacsScaffold 等。
- **新页面**：新增首页、统计页、备份页、设置页等完整页面体系。
- **主题系统**：建立统一的主题系统和图标库。
- **欢迎流程**：新增应用欢迎引导流程。
- **后端服务**：新增完整的 FastAPI 后端服务，包含 Agent 编排、模型客户端、安全模块等。

## 自测

- 后端 pytest 通过。
- Android gradlew test 通过。
- Android gradlew lintDebug 通过。
